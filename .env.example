# ============================================
# TLS/SSL Configuration (HTTPS)
# ============================================

# Your domain name (required for HTTPS with Let's Encrypt)
# Example: DOMAIN=example.com or DOMAIN=subdomain.example.com
# For local development without HTTPS, use: DOMAIN=localhost
DOMAIN=localhost

# Email for Let's Encrypt certificate notifications
# This email will receive expiration warnings and important updates
# Example: CERTBOT_EMAIL=admin@example.com
CERTBOT_EMAIL=admin@localhost

# Set to 1 to use Let's Encrypt staging server (for testing)
# Staging certificates are not trusted but have higher rate limits
# Set to 0 for production certificates (default)
CERTBOT_STAGING=0

# ============================================
# Database Configuration
# ============================================

POSTGRES_USER=user
POSTGRES_PASSWORD=password
DATABASE_URL=postgresql://user:password@postgres:5432/taca_ua # pragma: allowlist secret

# ============================================
# Object Storage Configuration
# ============================================

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# ============================================
# Identity and Access Management
# ============================================

KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# Keycloak Configuration for Application
KEYCLOAK_URL=http://keycloak:8080
KEYCLOAK_REALM=taca-ua
KEYCLOAK_CLIENT_ID=api-client
KEYCLOAK_CLIENT_SECRET=change-me-in-production

# ============================================
# Message Queue Configuration
# ============================================

RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# ============================================
# Django Configuration
# ============================================

# Generate a secure key for production:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY='example-secret-key' # pragma: allowlist secret
